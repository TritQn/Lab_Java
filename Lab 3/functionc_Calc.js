function funCalc(){
    var calculator = {
      read: function() {
        this.a = +prompt('Введите 1 переменную', 0);
        this.b = +prompt('Введите 2 переменную', 0);
      },
      sum: function() {
        return this.a + this.b;
      },
      mul: function() {
        s
      }
    }
      calculator.read();
      alert( calculator.sum() );
      alert( calculator.mul() );
}