function Age(){
    var age = prompt('Введите возраст');
    if ((age >= 14) && (age<=90))
    {
        alert('находится');
    }
    else
    {
        alert('не находится')        
    }
}
